CREATE DATABASE ECommerce_Sales;

USE  ECommerce_Sales;
CREATE TABLE Customers (
    Customer_ID INT PRIMARY KEY,
    Customer_Name VARCHAR(100),
    City VARCHAR(50)
);

CREATE TABLE Products (
    Product_ID INT PRIMARY KEY,
    Product_Name VARCHAR(100),
    Category VARCHAR(50),
    Price DECIMAL(10,2)
);

CREATE TABLE Orders (
    Order_ID INT PRIMARY KEY,
    Customer_ID INT,
    Product_ID INT,
    Quantity INT,
    Order_Date DATE,
    FOREIGN KEY (Customer_ID) REFERENCES Customers(Customer_ID),
    FOREIGN KEY (Product_ID) REFERENCES Products(Product_ID)
);

INSERT INTO Customers VALUES
(1,'Rahul Sharma','Mumbai'),
(2,'Priya Verma','Delhi'),
(3,'Arjun Reddy','Hyderabad'),
(4,'Sneha Patel','Bangalore'),
(5,'Kiran Kumar','Chennai'),
(6,'Anjali Singh','Pune'),
(7,'Vikram Rao','Hyderabad'),
(8,'Meera Joshi','Mumbai'),
(9,'Rohan Gupta','Delhi'),
(10,'Pooja Nair','Kochi');

INSERT INTO Products VALUES
(101,'Laptop','Electronics',55000),
(102,'Smartphone','Electronics',25000),
(103,'Headphones','Accessories',2000),
(104,'Keyboard','Accessories',1500),
(105,'Mouse','Accessories',800),
(106,'Smart Watch','Electronics',5000),
(107,'Tablet','Electronics',18000),
(108,'Monitor','Electronics',12000),
(109,'Printer','Electronics',9000),
(110,'USB Drive','Accessories',600);

INSERT INTO Orders VALUES
(1001,1,101,1,'2025-01-10'),
(1002,2,102,2,'2025-01-12'),
(1003,3,103,3,'2025-01-15'),
(1004,1,104,2,'2025-01-18'),
(1005,4,101,1,'2025-01-20'),
(1006,5,105,4,'2025-01-25'),
(1007,2,103,1,'2025-02-02'),
(1008,3,105,2,'2025-02-05'),
(1009,4,102,1,'2025-02-08'),
(1010,5,104,3,'2025-02-12'),
(1011,1,105,2,'2025-02-15');

SELECT * FROM CUSTOMERS;

SELECT  Customer_Name,CITY
FROM CUSTOMERS;

SELECT CUSTOMER_NAME 
FROM CUSTOMERS
WHERE CITY LIKE 'HYDERABAD';

SELECT COUNT(*) AS TOTAL_CUSTOMERS
FROM CUSTOMERS;

SELECT CITY, COUNT(*) AS TOTAL_CUSTOMERS
FROM CUSTOMERS
GROUP BY CITY;

SELECT CUSTOMER_NAME 
FROM CUSTOMERS
WHERE CUSTOMER_NAME LIKE 'A%';

SELECT * FROM CUSTOMERS
ORDER BY CUSTOMER_NAME ;

SELECT COUNT(CUSTOMER_ID) AS TOTAL_CUSTOMERS
FROM CUSTOMERS
WHERE CITY LIKE 'MUMBAI';

SELECT * FROM PRODUCTS;

SELECT PRODUCT_NAME,PRICE
FROM PRODUCTS;

SELECT * FROM PRODUCTS 
WHERE PRICE > 10000;

SELECT * FROM PRODUCTS
ORDER BY PRICE DESC
LIMIT 1;

SELECT * FROM PRODUCTS
ORDER BY PRICE ASC
LIMIT 1;

SELECT CATEGORY,COUNT(PRODUCT_NAME) AS TOTAL_PRODUCTS
FROM PRODUCTS
GROUP BY CATEGORY;

SELECT AVG(PRICE) AS AVERGAE_PRODUCT_PRICE
FROM PRODUCTs;

SELECT *
FROM ORDERS;

SELECT COUNT(*) AS TOTAL_ORDERS
FROM ORDERS;

SELECT * FROM ORDERS
WHERE  month(Order_Date) LIKE 2;

SELECT SUM(QUANTITY) AS TOTAL_QUANTITY_SOLD 
FROM ORDERS;

SELECT PRODUCT_ID, SUM(QUANTITY) AS TOTAL_QUANTITY_SOLD
FROM ORDERS
GROUP BY PRODUCT_ID
ORDER BY SUM(QUANTITY) DESC;

SELECT Customer_ID,COUNT(*) AS Total_Orders
FROM Orders
GROUP BY Customer_ID;

SELECT CUSTOMER_ID,SUM(QUANTITY) AS TOTAL_PURCHASED
FROM ORDERS
GROUP BY CUSTOMER_ID;

SELECT MAX(Quantity)
FROM Orders;

SELECT * FROM ORDERS
WHERE month(ORDER_DATE) > 1;

SELECT C.CUSTOMER_NAME, P.PRODUCT_NAME,O.QUANTITY,
O.ORDER_DATE
FROM CUSTOMERS C
JOIN ORDERS O
ON C.CUSTOMER_ID=O.CUSTOMER_ID
JOIN PRODUCTS P
ON P.PRODUCT_ID=O.PRODUCT_ID;

SELECT  SUM(O.QUANTITY*P.PRICE) AS TOTAL_REVENUE
FROM ORDERS O
JOIN PRODUCTS P
ON P.PRODUCT_ID = O.PRODUCT_ID;

SELECT C.CUSTOMER_NAME,SUM(P.PRICE*O.QUANTITY) AS TOTAL_SPENT
FROM CUSTOMERS C
JOIN ORDERS O
ON O.CUSTOMER_ID=C.CUSTOMER_ID
JOIN PRODUCTS P
ON P.PRODUCT_ID=O.PRODUCT_ID
GROUP BY C.CUSTOMER_NAME;

SELECT P. Category , SUM(P.PRICE*O.QUANTITY) AS TOTAL_REVENUE
FROM PRODUCTS P
JOIN ORDERS O
ON P.PRODUCT_ID=O.PRODUCT_ID
GROUP BY P.CATEGORY ;

SELECT C.CUSTOMER_NAME,SUM(P.PRICE*O.QUANTITY) AS TOTAL_SPENT
FROM CUSTOMERS C
JOIN ORDERS O
ON O.CUSTOMER_ID=C.CUSTOMER_ID
JOIN PRODUCTS P
ON P.PRODUCT_ID=O.PRODUCT_ID
GROUP BY C.CUSTOMER_NAME
ORDER BY SUM(P.PRICE*O.QUANTITY) DESC
LIMIT 1;

SELECT P.PRODUCT_NAME, SUM(O.QUANTITY*P.PRICE) AS TOTAL_REVENUE
FROM PRODUCTS P
JOIN ORDERS O
ON P.PRODUCT_ID=O.PRODUCT_ID
GROUP BY P.PRODUCT_NAME;

SELECT P.PRODUCT_NAME, SUM(O.QUANTITY*P.PRICE) AS TOTAL_REVENUE
FROM PRODUCTS P
JOIN ORDERS O
ON P.PRODUCT_ID=O.PRODUCT_ID
GROUP BY P.PRODUCT_NAME
ORDER BY SUM(O.QUANTITY*P.PRICE) DESC
LIMIT 1;

SELECT C.CUSTOMER_NAME , COUNT(O.ORDER_ID) AS TOTAL_ORDERS
FROM ORDERS O
JOIN CUSTOMERS C
ON C.CUSTOMER_ID=O.CUSTOMER_ID
GROUP BY O.CUSTOMER_ID
HAVING COUNT(O.ORDER_ID) > 1;

SELECT PRODUCT_NAME
FROM PRODUCTS
WHERE PRODUCT_ID NOT IN
(
    SELECT PRODUCT_ID
    FROM ORDERS
);

SELECT C.CITY,SUM(O.QUANTITY*P.PRICE) AS TOTAL_REVENUE
FROM CUSTOMERS C
JOIN ORDERS O
ON O.CUSTOMER_ID=C.CUSTOMER_ID
JOIN PRODUCTS P
ON P.PRODUCT_ID=O.PRODUCT_ID
GROUP BY C.CITY;